import {DeviceSessionAddType} from "../type/input-output-device-sessions";
import {randomUUID} from "crypto";

export const createDeviceSessionDto = (deviceSession:DeviceSessionAddType) => {
    //
    // return {
    //     ip: deviceSession.ip,
    //     title: deviceSession.title,
    //     lastActiveDate: deviceSession.lastActiveDate,
    //     deviceId:deviceSession.deviceId,
    //     expirationDate: deviceSession.expirationDate, // +1 день
    //     userId: deviceSession.userId,
    // }
}